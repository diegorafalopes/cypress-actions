name: cy actions tests

on: [push]

jobs:
  cy-electron-tests:
    runs-on: ubuntu-latest
    steps: 
      - name: Checkout
      - uses: actions/checkout@v4.2.2

      - name: install dependencies
      - run: yarn install

      - name: run e2e tests
      - run: yarn cypress run
